# HONGDAHIGHTECH Website

HDPTH foreign trade B2B website prepared for Vercel deployment.

## Deployment

```bash
npm install
npm run build
```

The build script copies the static website pages and assets into `public/`, then builds a minimal Next.js wrapper so Vercel can deploy the site with:

- Build command: `npm run build`
- Output directory: `.next`

